# CLAUDE.md

> Full project guidance is in **[AGENTS.md](./AGENTS.md)**.

This file exists as a Claude Code entry point. All guidance for this asset repository lives in `AGENTS.md` to keep a single source of truth shared across all AI tools.

## Claude-specific config

- Agents: `.claude/agents/` → symlink to `.agents/`
- Rules: `.claude/rules/` → symlink to `.rules/`
- Commands: `.claude/commands/` → symlink to `.prompts/`
